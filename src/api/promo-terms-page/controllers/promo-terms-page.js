'use strict';

/**
 * promo-terms-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::promo-terms-page.promo-terms-page');
