'use strict';

/**
 * sohnne-reward-term router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::sohnne-reward-term.sohnne-reward-term');
